#you must process this file using the source command because of BASHful reasons
#also you cannot set the environment from within make, so this step cannot be ran from a makefile
#to avoid this step add "./" to the start of your $PATH in .bashrc or .bash_profile (MacOS)

#Usage
#source path

export PATH=./transparency_commands:$PATH
echo $PATH
